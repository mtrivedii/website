{
    "name": "maanit-webapp-frontend",
    "version": "1.0.0",
    "description": "Maanit's Web Application Frontend",
    "scripts": {
      "build": "mkdir -p build && cp -r *.html js style.css staticwebapp.config.json build/"
    },
    "private": true
  }